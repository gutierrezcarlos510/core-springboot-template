# Copiar a .env y ajustar.

PORT=8080

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME={{service}}_db
DB_USER=postgres
DB_PASSWORD=postgres

APP_CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:*

# Si este backend habla con core-backend (auth, usuarios, instancias):
# CORE_API_URL=http://127.0.0.1:8080/api
# CORE_BASE_URL=http://127.0.0.1:8080/api

# GitHub Packages, solo si usás core-api-client:
# GITHUB_ACTOR=
# GITHUB_TOKEN=
